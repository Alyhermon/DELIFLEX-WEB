// Placeholder: app/layout.tsx importa este componente pero el archivo no
// existia (faltaba en el repo), lo que rompia `next dev`/`next build` para
// TODO el sitio, no solo /delipuntos. Se agrega vacio para no inventar el
// diseño real del widget de soporte - falta reemplazarlo por el definitivo.
export default function SupportWidget() {
  return null;
}
